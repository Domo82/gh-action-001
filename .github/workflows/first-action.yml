name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Greeting
      - id: print_greeting
        run: echo "Hello World"
      - name: Print Goodbye
        run: echo "Goodbye"
    
  
